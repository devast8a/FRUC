{expect} = require 'chai'

Grammar = require 'parser/grammar'
Any = require 'parser/matchers/any'

it 'sets optional false by default', ->

it 'sets optional true when one or more definitions are optional', ->
